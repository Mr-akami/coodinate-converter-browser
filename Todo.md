# TODO
- [x] `proj` のビルド方式を決定（Emscripten）。
- [x] `proj` を git submodule として追加（`third_party/proj`）。
- [x] `proj` Wasm ビルド用のスクリプトを作成。
- [x] Nix 開発環境（`flake.nix`）を用意。
- [x] OPFS への proj-data 展開フロー（Worker + バージョン管理）を実装。
- [x] `proj-data` 配布形式を tar.gz に決定し、パッケージ化手順を用意。
- [x] OPFS への展開フローを PoC（Worker + WasmFS 結線）。
- [x] `proj` のデータ検索パスを OPFS に向ける方法を検証。
- [x] `proj` C API を最小ラップした JS API を作成。
- [ ] ブラウザ起動時の「初回セットアップ」UX を設計。
- [ ] 2回目以降の高速起動パスを実装（展開済み判定）。
- [ ] 主要な座標変換ケースで検証テストを作成。
